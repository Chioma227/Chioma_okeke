enum ButtonVariant{
    DEFAULT,
}

enum containerVariants{
    MAIN,
    GRID,
    PFLEXED,
    FLEX,
    FLEXED,
    FLEX_COL,
    FLEX_START,
    FLEX_BTWN,
    MIN_WIDTH,
}
export {ButtonVariant, containerVariants}